# Atelier 2.11.25 — Approve buttons that show up, tidier skill headers

- **MCP "needs approval" now reliably offers an approve button.** A project (`.mcp.json`) server waiting on Claude's one-time approval shows the **approve** action instead of just "why?/reconnect" — the panel now re-checks approval state as it polls, not only when it first opened, so a server that starts needing approval (or was approved elsewhere) reflects promptly.
- **Skills header buttons aligned.** The **enable** / **enable all** action on a pack's section now sits properly at the right edge instead of floating over the label.
